#ifndef __GEOTYPES_HPP__
#define __GEOTYPES_HPP__

//TODO: This file seems excessive. Figure out a better place for these.
#include <vector>
using namespace std;

typedef double Scalar;
///a location in vector.size() dimensions
typedef vector<Scalar> Coordinate;
///the size an object spans in each of vector.size() dimensions
typedef vector<Scalar> Size;

#endif
